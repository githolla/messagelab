# Server-side only — set in Vercel project settings, never committed.
ANTHROPIC_API_KEY=sk-ant-...
# Optional model override (defaults to claude-sonnet-4-6)
MESSAGE_LAB_MODEL=
# Optional: local Chromium path for the URL page-capture in "Review a page".
# Not needed on Vercel (uses @sparticuz/chromium) or if you only upload screenshots.
CHROME_PATH=
